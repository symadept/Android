apply plugin: 'com.android.instantapp'

android {
    compileSdkVersion 26

    buildTypes {}

    repositories {
        flatDir {
            dirs '../base/libs'
        }
    }
}

dependencies {
    implementation project(':base')
    implementation(name: 'details-debug', ext: 'aar')
}
